# Company VPN setup

Install the WireGuard desktop client from the managed software catalogue. Import the company profile, select the `Praxis-Corporate` tunnel, and connect before opening internal services.

If the tunnel does not connect, confirm that the profile is current, reconnect to the internet, and retry. Contact the service desk only after recording the WireGuard error message.
